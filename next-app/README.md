# Kream Proxies Next App

App Router migration of the Kream Proxies landing page.

```bash
pnpm install
pnpm dev
pnpm lint
pnpm build
```

The landing page sections live in `src/app/_components`, shared UI primitives live in
`src/components`, and global theme styles live in `src/app/globals.css`.
